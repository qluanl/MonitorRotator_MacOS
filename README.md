# Rotate the monitor on MacOS

### A command line tool to rotate monitors.

### Usage: 
`./MonitorRotator` check the connected monitors.
`./MonitorRotator 1 90` rotate the first external monitor to 90 deg configuration.
